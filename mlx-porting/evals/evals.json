{
  "skill_name": "mlx-porting",
  "evals": [
    {
      "id": 3,
      "name": "parity-harness-setup",
      "prompt": "J'ai porté un VAE 3D de PyTorch vers MLX. L'output end-to-end est très bruité (on reconnaît à peine les formes). Je soupçonne un bug de port mais je ne sais pas où. Aide-moi à mettre en place un harness de parité PT↔MLX pour isoler la couche fautive. Donne-moi la structure de fichiers, un template de test, et une stratégie d'isolation.",
      "expected_output": "tests/parity/ layout, torch optional dep, fp32 thresholds, bisection, seeded fixtures, references bundled parity_helpers.py",
      "files": []
    },
    {
      "id": 5,
      "name": "scaffold-new-mlx-fork",
      "prompt": "Je démarre le portage MLX d'un nouveau modèle diffusion image→video appelé 'MotionForge' (hypothétique, DiT transformer + T5 text encoder + VAE 2D+1D). Je n'ai encore rien écrit. Je veux scaffolder le fork `MotionForge-mlx` proprement. Donne-moi :\n\n1. La liste exacte des fichiers à créer avec leur chemin (répertoire + nom) et un one-liner sur ce que chacun contient.\n2. Un `pyproject.toml` complet avec les bonnes dépendances (runtime + dev + parity) — et explique pourquoi torch est séparé.\n3. Le squelette de `pipeline_mlx.py` montrant le pattern `from_pretrained(repo_id)` avec auto-download HF et override env-var.\n4. Le squelette de `tests/parity/_helpers.py` ou équivalent réutilisable.\n5. Comment brancher sur `mlx-forge` pour la conversion et sur `mlx-arsenal` pour les ops réutilisables.\n\nJe veux un starter kit que je puisse commiter dès demain.",
      "expected_output": "Full fork scaffolding with repo-layout convention, pyproject parity extra, env-var override, parity helpers pointer, mlx-forge/mlx-arsenal handoff guidance.",
      "files": []
    },
    {
      "id": 6,
      "name": "multi-bug-debugging",
      "prompt": "Voici un extrait de port MLX d'un UNet2D diffusers (SD 1.5 style). L'output du modèle est gris-cyan neutre, quasi pas structuré — bien pire qu'un port bugué habituel. Diagnostique TOUS les bugs (il y en a plusieurs) et classe-les par probabilité d'être la cause racine.\n\n```python\n# Config from the source repo's config.json:\n# block_out_channels: [320, 640, 1280, 1280]\n# attention_head_dim: [8, 8, 8, 8]   # diffusers-style\n# cross_attention_dim: 768\n# norm_num_groups: 32\n\nclass CrossAttention(nn.Module):\n    def __init__(self, channels, cross_dim=768, num_heads=8):\n        super().__init__()\n        self.num_heads = num_heads\n        self.head_dim = channels // 8  # hardcoded 8\n        self.to_q = nn.Linear(channels, channels, bias=False)\n        self.to_k = nn.Linear(channels, channels, bias=False)\n        self.to_v = nn.Linear(channels, channels, bias=False)\n        self.to_out = nn.Linear(channels, channels)\n\n    def __call__(self, x, context):\n        B, N, C = x.shape\n        q = self.to_q(x).reshape(B, N, self.num_heads, self.head_dim).transpose(0, 2, 1, 3)\n        k = self.to_k(context).reshape(B, -1, self.num_heads, self.head_dim).transpose(0, 2, 1, 3)\n        v = self.to_v(context).reshape(B, -1, self.num_heads, self.head_dim).transpose(0, 2, 1, 3)\n        out = mx.fast.scaled_dot_product_attention(q, k, v)\n        out = out.transpose(0, 2, 1, 3).reshape(B, N, C)\n        return self.to_out(out)\n```\n\nLes poids ont été chargés sans erreur via `mx.load` puis `model.update(tree_unflatten(list(weights.items())))`. Que faut-il corriger ?",
      "expected_output": "Diagnose: wrong kv dim, missing scale, to_out shape missing Sequential, materialization check. Ranked by root-cause probability. Suggest parity verification.",
      "files": []
    },
    {
      "id": 7,
      "name": "weight-conversion-delegation",
      "prompt": "Je dois convertir les poids PyTorch de 'MotionForge' (DiT + T5 + VAE) en MLX safetensors split par composant. Tu peux me donner la recette mlx-forge complète — classify_key, sanitize_key, convert orchestration, quantization scope ?",
      "expected_output": "Points to mlx-recipe skill for full authoring. Describes pattern (classify/sanitize/convert), materialization-before-save rule, per-component split, quantization scope (transformer Linear only).",
      "files": []
    },
    {
      "id": 8,
      "name": "fast-primitives-and-quant-strategy",
      "prompt": "J'implémente un transformer block MLX pour un nouveau LLM que je porte. Voici ce que j'ai écrit pour un seul block :\n\n```python\nclass TransformerBlock(nn.Module):\n    def __init__(self, dim, n_heads, n_kv_heads, head_dim, rope_base=10000.0):\n        super().__init__()\n        self.n_heads = n_heads\n        self.n_kv_heads = n_kv_heads\n        self.head_dim = head_dim\n        self.wq = nn.Linear(dim, n_heads * head_dim, bias=False)\n        self.wk = nn.Linear(dim, n_kv_heads * head_dim, bias=False)\n        self.wv = nn.Linear(dim, n_kv_heads * head_dim, bias=False)\n        self.wo = nn.Linear(n_heads * head_dim, dim, bias=False)\n        self.attn_norm = nn.RMSNorm(dim, eps=1e-6)\n        self.ffn_norm = nn.RMSNorm(dim, eps=1e-6)\n        self.w1 = nn.Linear(dim, dim * 4, bias=False)\n        self.w2 = nn.Linear(dim * 4, dim, bias=False)\n        self.w3 = nn.Linear(dim, dim * 4, bias=False)\n        # RoPE cache computed manually\n        freqs = 1.0 / (rope_base ** (mx.arange(0, head_dim, 2) / head_dim))\n        self.freqs = freqs\n\n    def __call__(self, x, pos):\n        B, N, _ = x.shape\n        h = self.attn_norm(x)\n        q = self.wq(h).reshape(B, N, self.n_heads, self.head_dim).transpose(0, 2, 1, 3)\n        k = self.wk(h).reshape(B, N, self.n_kv_heads, self.head_dim).transpose(0, 2, 1, 3)\n        v = self.wv(h).reshape(B, N, self.n_kv_heads, self.head_dim).transpose(0, 2, 1, 3)\n\n        # Manual RoPE\n        cos = mx.cos(pos[:, None] * self.freqs[None, :])\n        sin = mx.sin(pos[:, None] * self.freqs[None, :])\n        cos = mx.repeat(cos, 2, axis=-1)\n        sin = mx.repeat(sin, 2, axis=-1)\n        q1, q2 = q[..., :self.head_dim//2], q[..., self.head_dim//2:]\n        q_rotated = mx.concatenate([q1 * cos[..., :self.head_dim//2] - q2 * sin[..., :self.head_dim//2], q2 * cos[..., self.head_dim//2:] + q1 * sin[..., self.head_dim//2:]], axis=-1)\n        k_rotated = q_rotated  # (same pattern for k, omitted for brevity)\n\n        # Manual GQA: repeat K/V to match Q head count\n        k_rotated = mx.repeat(k_rotated, self.n_heads // self.n_kv_heads, axis=1)\n        v = mx.repeat(v, self.n_heads // self.n_kv_heads, axis=1)\n\n        # Attention: manual\n        scores = q_rotated @ k_rotated.transpose(0, 1, 3, 2) / mx.sqrt(self.head_dim)\n        mask = mx.tril(mx.ones((N, N)))\n        scores = mx.where(mask, scores, -1e9)\n        attn = mx.softmax(scores, axis=-1)\n        out = attn @ v\n        out = out.transpose(0, 2, 1, 3).reshape(B, N, -1)\n        out = self.wo(out)\n\n        h = x + out\n        ffn = self.ffn_norm(h)\n        h2 = h + self.w2(nn.silu(self.w1(ffn)) * self.w3(ffn))\n        return h2\n```\n\nDeux questions :\n1. Ce block fonctionne mais je soupçonne que je n'utilise pas les bonnes primitives MLX. Quelles primitives de `mlx.fast` devrais-je utiliser à la place de mon code manuel et pourquoi ?\n2. Quand je vais quantizer ce modèle (int4 g=64), quelle est la bonne scope — tout, juste les Linears du transformer, ou autre ? Justifie.",
      "expected_output": "Recommends mx.fast.rope (replaces manual cos/sin rotation), mx.fast.scaled_dot_product_attention (replaces manual QK^T/scale/mask/softmax/V + manual GQA repeat — MLX handles GQA natively with unequal head counts), mx.fast.rms_norm (replaces nn.RMSNorm for hot path). Quant scope: ONLY Linear.weight in transformer blocks (wq/wk/wv/wo/w1/w2/w3). Keep embedding, norm weights, RoPE cache fp16/bf16. Explains why (numerics sensitivity for norms/embeddings, perf gain from fast primitives).",
      "files": []
    }
  ]
}
